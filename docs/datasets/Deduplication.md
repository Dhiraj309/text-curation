## Semantics and Scope

Deduplication operates on the dataset as provided.

The deduplication report reflects the exact representation
of the data at the time the operation was applied.

If text is later transformed (for example, via a curation
profile), the deduplication report does not change and
does not attempt to reflect post-transformation content.

No enforcement or automatic re-deduplication is performed.
